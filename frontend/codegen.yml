overwrite: true
schema: "http://localhost:4000"
documents: "lib/**/*.{tsx,ts}"
generates:
  interfaces/graphql_generated.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
